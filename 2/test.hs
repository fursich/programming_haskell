-- no main function included. this program is for gchi use
double x = x + x
quadruple x = double(double x)

factorial n = product [1..n]
average ns = sum ns `div` length ns

{-
  this part has to be considied as
  lines of comments, and it is.
-}
